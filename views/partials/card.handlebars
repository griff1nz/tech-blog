{{!-- <p>{{title}} by {{user.username}}, {{comments.length}} comments</p>
--}}
<a href="/posts/{{id}}">
    <div class="border-1 border-black m-2 bg-slate-200 hover:bg-slate-500 transition ease-in-out rounded-sm">
        <div class="p-2 flex justify-between">
            <div>
                <h1 class="text-4xl">{{title}}</h1>
                <p>{{comments.length}} comments</p>
            </div>
            <p class="text-lg">By {{user.username}} on {{date}}</p>
        </div>
    </div>
</a>